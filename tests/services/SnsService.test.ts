// import { SnsService } from "../../src/services/SnsService";
// import { defaultCreateInstanceOptions } from "../../src/createInstance";

// describe("SnsService", () => {
//   const snsService = new SnsService(defaultCreateInstanceOptions.sns);

//   describe("isSupported", () => {
//     it("support 'sns.key'", () => {
//       expect(snsService.isSupported("abc.bnb")).toBe(true);
//     });

//     it("do not support sns.key", () => {
//       expect(snsService.isSupported("abc.bit")).toBe(false);
//     });
//   });
// });
